prql target:sql.sqlite

from (read_csv "genbank_metadata.tsv" )
filter (
    `Isolate Collection date` | text.length >= 10
)
filter (
    `Isolate Collection date` | text.length != 0
)
filter (
    `Geographic location` | text.length != 0
)
